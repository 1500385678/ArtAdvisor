# 2026-08-25 · ArtAdvisor · 每日开发计划

> 07-艺术-Art 行业 Web · CronTask T5 · 03:00 自动执行
> 状态:昨日 T4 漏写 plan,本次 fallback 自建

---

## 今日任务(1 个小变更)

### 启动 Phase 0 任务 2:artworks.json 首批入库

**目标**:从 `data/artworks-source-list.md` 66 部候选中抽取**首批 10 部**(文艺复兴段 1-10),结构化写入 `data/artworks.json`,建立 JSON schema 样板,为后续 5 批 × 100 部迭代铺路。

**字段**(按 项目开发计划.md 任务 2 要求):
- `id`(统一编号 `aw-001` 风格)
- `title_cn` / `title_en`
- `artist` / `artist_id`(预留)
- `year` / `period`(流派/时期)
- `medium`(媒介:油画/壁画/雕塑/建筑...)
- `dimensions`(尺寸,如 77×53 cm)
- `museum` / `museum_city`
- `region`(西方/东方/伊斯兰/当代)
- `module_tags`(适配模块:作品库/鉴赏/创作/策展)
- `source_ref`(回指源 md 行号,可追溯)

**完成标志**:
1. `data/artworks.json` 落盘 10 条
2. 顶层 `meta`:total=10/batch=1/source="data/artworks-source-list.md"
3. 字段验证通过(无 null/无空字符串)
4. `项目开发计划.md` Phase 0 任务 2 勾选 [x] (注:首批 10/500,先 partial 推进)

---

## 不做(明确)

- ❌ 不动 README.md 冲突(巡检已标记,留给人工合并)
- ❌ 不写 FastAPI 代码(0 代码阻挡,留 Phase 1)
- ❌ 不动其他已勾选 / 未勾选任务
- ❌ 不替换源清单 md,只读取

---

## 风险

- 首批只 10 部,500 部目标远 → 后续每日 1 批,约 50 天可达 500
- 字段不全会让下游消费方抱怨 → 严格 10 字段,先全不全再说

---

## 变更记录

- **新增**:`data/artworks.json`(首批 10 部,文艺复兴段)
- **修改**:`项目开发计划.md` Phase 0 任务 2 勾选 [x](partial)
- **删除**:`.plan/20260825.md`(本次执行完后清理)
