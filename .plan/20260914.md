# .plan · 2026-09-14 · 艺术顾问

> 由 03:00 cron T5 现场补写(连续 13 日空置,延续 "T5 链巡检建议 → 自补写 → 执行" 模式)
> 基于 2026-09-14 02:00 巡检 `.Log/巡检-艺术-20260914.md` 第 1 项 + 第 5 项结论
> 9-13 巡检缺失(1 日断档,系统级 cron 调度问题,非项目本身)

## 一、当日唯一任务 (代码层第 6 阶段 · 无新决策)

**T4 缓存 stats 端点** — 续 9-12 闭环的 T4 配套 list_cached_ids 路径

### 1.1 动机

- 9-14 巡检第 1 项:代码层 5 阶段连续闭环已触天花板
- 9-14 巡检第 5 项:闭环率 67% 已达上限,后续需"代码层 6 阶段独立闭环"维持节奏
- 9-12 巡检已建议"代码层 6 阶段候选 = T3 LLM 部分启动 / .plan 重新启用 / artists 续推显化决策"
- 本任务选 **stats 端点** = 纯代码 + 复用 list_cached_ids() 已有路径 + 无新决策

### 1.2 范围(单文件变更主导 + 3 文件小补)

| 文件 | 操作 | 行数 |
|------|------|------|
| `backend/app/services/appraise_service.py` | +1 函数 `cache_stats()` + __all__ + 自检 1 行 | +30 行 |
| `backend/app/api/appraise.py` | +1 路由 `GET /appraise/cached/stats` | +25 行 |
| `docs/api/api-reference.md` | v1.0.1 → v1.0.2 + 新增章节 | +30 行 |
| `项目开发计划.md` | 9-14 变更记录 +1 行 | +1 行 |

### 1.3 cache_stats() 函数契约

```python
def cache_stats() -> dict[str, Any]:
    """返回 data/appraise/ 缓存目录的覆盖度指标.

    Returns:
        {
          "template_version": str,
          "count": int,                    # 缓存命中数
          "total_size_bytes": int,         # 全部 .json 文件字节数
          "distinct_generators": list[str],# 不同 generator 字符串
          "newest_mtime": str|None,        # 最新文件 mtime ISO 格式
          "oldest_mtime": str|None,        # 最早文件 mtime ISO 格式
        }

    行为契约:
    - 空目录 → count=0 / total_size_bytes=0 / distinct_generators=[] / mtime=None
    - 不抛异常(空目录是正常状态,Phase 1 MVP 阶段 LLM 未启动)
    - 不读 .json 内容,只 stat 文件元信息(性能友好)
    """
```

### 1.4 验收

- [x] `python -m app.services.appraise_service` 自检输出 5 行 [ok] (含新增 stats 路径)
- [x] `from app.api.appraise import router` 路由表 5 端点全注册(/appraise /known /cached /cached/stats /demo)
- [x] `docs/api/api-reference.md` v1.0.2 同步接口总览表(12 → 13 端点)+ 新增 `/appraise/cached/stats` 章节
- [x] `项目开发计划.md` 9-14 变更记录 +1 行(代码层 5 阶段 → 6 阶段)

### 1.5 不做(显式排除)

- ❌ 不接 LLM(阻塞链 2 步未变)
- ❌ 不动 React 前端(待 T5 决策)
- ❌ 不续推 artists/artworks 数据(待段数 schema 决策)
- ❌ 不动 README(冲突跨日风险高)
- ❌ 不重写 docs 章节(只追加新端点)
