# 2026-09-16 开发计划 · 07-艺术-Art

> 当日 T4(02:00 巡检时未写入) + T5(03:00 cron 兜底) · 1 个代码层无决策依赖子任务

## 来源
9-16 巡检(`.Log/巡检-艺术-20260916.md`)第 1 项风险建议:
- "代码层 + 文档层可独立闭环阶段已触 7 阶段天花板"= 9-8 services / 9-9 T2 / 9-10 API 参考 / 9-11 T4 save+load / 9-12 T4 配套 list+cached / 9-14 T4 配套 stats+cached/stats / 9-15 README 同步
- "代码层第 8 阶段候选 = T3 LLM prompt 骨架(纯字符串 + 构建函数,不调模型,无决策)"

## 今日 1 个小变更 · T3 LLM prompt 骨架(代码层第 8 阶段)

**任务**:新增 `backend/app/services/prompts.py`,落地 5 维 prompt 骨架字符串 + `build_appraise_prompt(artwork_meta, *, focus_dim=None)` 构建函数 + `parse_llm_output(raw)` JSON 解析容错函数 + 自检脚本。

**理由(为何不冲突决策)**:
- 阻塞链 T3 LLM 选型 3 重决策中(模型选型 / prompt 填充策略 / schema 字段映射规则),仅"prompt 填充策略"可代码层独立闭环
- "模型选型(Claude / GPT-4o / Qwen-VL)"待张勇决策,本任务不调任何模型
- "schema 字段映射规则"已在 `docs/鉴赏讲解模板.md` + `TEMPLATE_SCHEMA` 落盘,本任务复用,不再独立决策

**产出**:
- `services/prompts.py` 约 80 行(prompt 字符串 ~30 行 + build_appraise_prompt ~15 行 + parse_llm_output ~20 行 + 自检 ~15 行)
- 1 行 README 阶段状态表增量:把"代码层 7 阶段"升级为"代码层 8 阶段"
- 主计划变更记录 +1 行

**验收**:`python -m app.services.prompts` 输出 3 行 `[ok]`(build_appraise_prompt 构造 / parse_llm_output 合法 JSON 解析 / parse_llm_output 非法输入容错)

## 不做什么
- 不调用任何 LLM(模型选型决策未显化)
- 不续推 artists / artworks(数据集层决策未显化)
- 不启动 T5 前端(脚手架决策未显化)
- 不写 `requirements.txt` 增量(无新依赖)
- 不修任何既有代码(纯追加 services/prompts.py)

## 推进清单
1. [x] 写 `.plan/20260916.md`(本文件)
2. [x] 写 `backend/app/services/prompts.py` + 自检
3. [x] `python -m app.services.prompts` 自检通过
4. [x] README 阶段状态表 Phase 1 行加"代码层 8 阶段"
5. [x] 主计划 9-16 变更记录 +1 行(8 阶段闭环)
6. [x] git add -A + push gitee + push github
7. [x] 删除临时 `.plan/20260916.md`(留 commit 痕迹)
8. [x] 报告 commit hash + Gitee/GitHub 状态