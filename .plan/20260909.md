# Plan · 07-艺术-Art · 2026-09-09

> 9-9 巡检 12 项风险中"按建议采纳率回升"自恢复机制在 services(代码层)单维度闭环成功
> 印证 9-7/9-8 巡检"自恢复机制仅适用于不涉及新决策的续推任务"判断的部分有效性
> 本轮锁定**代码层可独立启动**子任务,不触碰需新决策的数据集层(artists 续推 / artworks 批次 8 / 段数 schema 拆分)

---

## 今日变更(单任务)

### 任务:T2 /appraise 接口联通(Phase 1 MVP 鉴赏讲解阻塞链第 2 步)

**承接**:9-8 03:00 commit `b0ec730` T1 闭环(services/appraise_service.py 275 行,5 维模板静态加载 + aw-001 demo + evaluate() 公开 API),T2 接口联通仍 15 行桩,**Phase 1 MVP 验收"10 作品鉴赏 demo"阻塞链从 5 步演化为 4 步**

**目标**:改写 `backend/app/api/appraise.py` 联通 `services.appraise_service.evaluate()`,对外暴露 5 维 JSON

**端点设计**(参照 gallery.py 风格,Phase 1 最小可用集):
- `GET /appraise?artwork_id=aw-001` → 调用 evaluate() 返回 5 维 JSON(主端点)
- `GET /appraise/known` → 返回 list_known_ids()(当前可产出 5 维的 artwork_id 清单)
- `GET /appraise/demo` → 返回 aw-001 demo(快速预览,无 query 参数)

**不触碰**(本轮不启动):
- T3 LLM 调用(需决策:Claude / GPT-4o,prompt 骨架填充策略)
- T4 缓存到 data/appraise/{id}.json(需决策:缓存粒度 / 失效策略)
- T5 前端 5 维分 Tab(需 React 工程)
- artists 续推 ar-074+(需段数 schema 决策)
- artworks 批次 8(需段数 schema 决策)

**验收**:
- `python -m app.services.appraise_service` 仍通过(无回归)
- 改写后 `appraise.py` 可被 main.py 注册(`app.include_router(appraise_router)` 已在)

---

## 不在本轮范围

- 巡检建议采纳清单 11 项中 artists/artworks/段数 schema 三项需张勇决策,9-10 03:00 巡检再观察
- 9-9 巡检自身已落盘,本轮不重复生成

---

> 计划写入:T4 2026-09-09 02:30 | 执行:03:00 cron | 删除:推送完成后
